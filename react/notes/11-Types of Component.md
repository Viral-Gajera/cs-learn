## Pure component :

- To stop unnecessary rendaring the components.
- Only rendars the conponent when state or props has change.

```JavaScript
class App extends React.PureComponent
{
	// body
}
```

## Higher order component :

- Component which takes other components as argument ( form props ) or return other component is called HOC.

```
```

