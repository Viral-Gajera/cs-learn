## Inheritance

- Deriving new class form existing class is called inheritance.
- Inheritance used to reuse the code.
- Multiple inheritance is not possible in javaScript.
- Javascript supports `protype-based` inheritance rather than class-based.
    
    ![[4 3.jpg|4 3.jpg]]
    

### Prototype :

- Each object has a property which holds a link to another object called its `prototype`.
- The 'object' from which other object inherites property, is known as the `prototype object`.
- The 'prototype object' contains methods and property that are accessible to all object linked to that prototype.
- That 'prototype object' can have it's own prototype, and so on until an object is reached with 'null' as its prototype. it also forms prototype chain.
- When we read a property from object, and it’s missing, JavaScript automatically search it, in it's prototype, and it's prototype and so on until it reaches to 'null'. this is called “prototypal inheritance”.
- In javaScript, nearly, all objects inherit from at least one object which is, `Object.prototype` which has `null` as its prototype. `Object.prototype.__proto__ = null`

### Accessing ProtoType object :

- The following properties/Methods are used to access the prototype object of any object.
    
    ```Plain
    objectName.__proto__	// used to access/manipulate prototype object.
    
    Object.getPrototypeOf(obj)
    Object.setPrototypeOf(obj, prototypeobj)
    ```
    
- It should not be confused with the `func.prototype` property of functions.
- All functions have a special property named `prototype`, which specifies the [[Prototype]] to be assigned to all instances of objects created by the given function when used as a constructor.
- Example :
    
    ```Plain
    const Person = function(name, birthYear)
    {
    	this.name = name;
    	this.birthYear = birthYear;
    }
    
    Person.prototype.calcAge = function()
    {
    	return ( new Date().getFullYear() ) - this.birthYeat;
    }
    ```
    

![[5 3.png|5 3.png]]

  

- Example :
    
    Following Example shows defining property or methods to prototype object and use of `__proto__` property.
    
    ```Plain
    const F = function () {
        this.a = 1;
        this.b = 2;
     };
    
    F.prototype.b = 3;				// defining property to prototype object
    F.prototype.c = 4;				// defining property to prototype object
    F.prototype.Func = function()	// defining method to prototype object
    {
        console.log( ` a : ${this.a}, b : ${this.b}, c : ${this.c} ` );
    };
    
    const o = new F(); 	// { a: 1, b: 2 }
    
    console.log(o.a); 	// 1
    console.log(o.b); 	// 2
    console.log(o.c); 	// 4
    console.log(o.d); 	// undefined
    o.Func();         	// a : 1, b : 2, c : 4
    console.log(o.__proto__); //{b: 3, c: 4, Func: ƒ}
    ```
    
- Example :
    
    Defining prototype object using object literal.
    
    use `__protp__` : 'prototype object'; " property to define the prototype object of object literal.
    
    ```Plain
    let o = {
        fullName : function (){
            return `${this.firstName} ${this.lastName}`
        }
    }
    
    let obj = {
        __proto__ : o,
        firstName : 'viral',
        lastName : 'gajera'
    }
    
    console.log(obj);               //{firstName: 'viral', lastName: 'gajera'}
    console.log(obj.__proto__);     //{fullName: ƒ}
    ```
    
- Example :
    
    `Object.create(prototypeObj)` method creates a new object, with prototypeObj as prototype of newly created object.
    
    ```Plain
    const a = { a: 1 };
    // a ---> Object.prototype ---> null
    
    const b = Object.create(a);
    // b ---> a ---> Object.prototype ---> null
    console.log(b.a); // 1 (inherited)
    
    const c = Object.create(b);
    // c ---> b ---> a ---> Object.prototype ---> null
    
    const d = Object.create(null);
    // d ---> null
    ```
    

### Implement prototype inheritance :

- Constructor function
- ES6 classes
- Object.create()

### (i) constructor function:

- The following syntax is used to link prototypes of parent constructor to prototype of child constructor.
    
    ```Plain
    childConstructor.prototype = Object.create(parentConstructor.prototype);
    childConstructor.prototype = {...parentConstructor.prototype};
    ```
    
- We use `parentConstructor.call(this, args...)` to call the parent function constructor from child constructor.
- Example :
    
    ```Plain
    // Person constructor:
    function Person(firstName, lastName){
        this.firstName = firstName;
        this.lastName = lastName;
    }
    
    Person.prototype.fullName = function(){
        return `${this.firstName} ${this.lastName}`;
    }
    
    
    // Student construtor:
    function Student(firstName, lastName, EnNo){
        // calling parent function constructor
        Person.call(this, firstName, lastName);
    
        this.EnNo = EnNo;
    
    }
    
    // Linking prototypes
    Student.prototype = Object.create(Person.prototype);
    
    // setting constructor of student.prototype to student
    Student.prototype.constructor = Student;
    
    Student.prototype.details = function(){
        return `${this.firstName} ${this.lastName} has enrollment number, ${this.EnNo}.`;
    }
    
    let s1 = new Student('viral', 'gajera', 92000103080);
    
    console.log(s1 instanceof Student); //true
    console.log(s1 instanceof Person);  //true
    									// false if prototype is not linked
    
    console.log(s1 instanceof Object);  //true
    //Person.protoype object inherites properties from Object.prototype object.
    ```
    
    ![[6 4.png|6 4.png]]
    

### (ii) Classes :

- `extends` keyword used to link `parentFunc.prototype` to `childFunc.prototype`.
- `super` keyword used to calls the parent constructor from child constructor.
- Here is Example of inheritance using class and equivalent constuctor fucntion code.
    
    ```Plain
    class Person
    {
        constructor(firstName, lastName){
            this.firstName = firstName;
            this.lastName = lastName;
    
    
    	/* we can also define method here.*/
    	}
    	/*
    	    equivalent to
    	    function Person(firstName, lastName){
    	            this.firstName = firstName;
    	            this.lastName = lastName;
    	    }
    	*/
    
    	fullName(){
    	    console.log( `${this.firstName} ${this.lastName}` );
    	}
    	/*
    	    equivalent to
    	    Person.prototype.fullName = funcntion (){
    	    	console.log( `${this.firstName} ${this.lastName}` );
    	    }
    
    	*/
    
    }
    
    class Student extends Person{
        /*
            equivalent to
    	Student.prototype = Object.create(Person.prototype);
        */
    
        constructor(firstName, lastName, rollNo){
            super(firstName, lastName);
        	/* Person.call(this, firstName, lastName); */
        	// it should be always first statement and must be invoke, error otherwise.
    
            this.rollNo = rollNo;
        }
    
        printStudent(){
            console.log(`${this.firstName} ${this.lastName} ${this.rollNo}`);
        }
    
    }
    
    let obj = new Student('viral', 'gajera', 92000103080);
    
    obj.fullName();
    obj.printStudent();
    ```
    

### (iii) Using Object.create() :

- Following Example shows inheritance using only objects. (without constuctor function)
    
    Example :
    
    ```Plain
    const PersonProto = {
        calcAge() {
          console.log(2022 - this.birthYear);
        },
    
        init(firstName, birthYear) {
          this.firstName = firstName;
          this.birthYear = birthYear;
        }
    
    };
    
    const StudentProto = Object.create(PersonProto);
    
    StudentProto.init = function (firstName, birthYear, course)
    {
        PersonProto.init.call(this, firstName, birthYear);
        this.course = course;
    };
    
    StudentProto.introduce = function () {
        console.log(`My name is ${this.firstName} and I study ${this.course}`);
    };
    
    const viral = Object.create(StudentProto);
    viral.init('viral', 2002, 'Computer Science');
    viral.introduce();
    viral.calcAge();
    ```
    

### Inheriting Methods and `this` :

- When an inherited function is executed, the value of `this` points to the inheriting object, not to the prototype object where the function is an own property.
- ==No metter where the method is founds, in object or it's prototype. In a method call 'this' always referes to object before the dot(.)==
    
    ```Plain
    const o = {
      a: 2,
      m: function() {
        return this.a;
      }
    };
    
    console.log(o.m()); // 2
    // When calling o.m in this case, 'this' refers to o
    
    const p = Object.create(o);
    // p is an object that inherits from o
    
    p.a = 4; // assign the value 4 to the property 'a' on p
    console.log(p.m()); // 4
    // when p.m is called, 'this' refers to p.
    // 'this.a' means p.a, the property 'a' of p
    ```
    

### limitations of prototype :

- The references can’t go in circles. JavaScript will throw an error if we try to assign `__proto__` in a circle.
- The value of `__proto__` can be either an object or null. Other types are ignored.
- There can be only one [[Prototype]], An object may not inherit from two others.

## Polymorphism :

- Polymorphism is the presentation of one interface for multiple data types.
- For example, integers, floats, and doubles are implicitly polymorphic: regardless of their different types, they can all be added, subtracted, multiplied, and so on.